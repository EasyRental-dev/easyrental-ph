# Copy to Vercel Project → Settings → Environment Variables
TELEGRAM_BOT_TOKEN=123456789:AAExampleFromBotFather
TELEGRAM_CHAT_ID=123456789

# Auto-log web form leads as Inquiries in the rental app (optional but recommended)
# Use the same values as Flutter --dart-define=GAS_URL and API_SECRET
GAS_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
API_SECRET=your_hmac_secret
# GAS_DEVICE_ID=WEB-FORM-BOT

# Optional: restrict contact API to your production domain(s), comma-separated
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
